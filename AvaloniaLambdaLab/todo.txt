1. Fix code so that it saves selected options.
   - Startup loads the last selected options.
   - On close saves them
2. Add list view so it displays multiple instances running.
   - Add collection with the running instances.
   - Add stop bottom to stop selected instance.
   
3. Spawn an instance.
    - Add confirmation dialog.
    - Add options to select instance options.
4. Work on graphings.
    - Iterate through running instances.
    - Collect CPU, RAM, Network usage.
    - Display them in a graph.
    - Add to selected instance details view.

